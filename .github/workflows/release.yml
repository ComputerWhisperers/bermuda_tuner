name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Package integration
        working-directory: custom_components/bermuda_tuner
        run: zip -r ../../bermuda_tuner.zip . -x "*/__pycache__/*" "*.pyc"
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: bermuda_tuner.zip
